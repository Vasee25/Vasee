PORT=3000
SESSION_SECRET=replace-with-a-long-random-secret
MAX_FILE_SIZE_MB=25

# Optional SMTP configuration. If omitted, email notifications are logged to the server console.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Document Management <no-reply@example.com>"
